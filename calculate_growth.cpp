template<class Type>
vector<Type> calculate_growth(vector<Type>EReproAndGrowth, Type w_mat, Type U,
                              vector<Type> bin_boundaries)
{
    int size = bin_boundaries.size();
    vector<Type> growth(size) = EReproAndGrowth; \\ TODO: Add growth calculation

    return growth;
}
